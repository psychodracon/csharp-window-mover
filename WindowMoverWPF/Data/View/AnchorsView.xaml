<UserControl x:Class="WindowMoverWPF.Data.View.AnchorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowMoverWPF.Data.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Anchors"
                   Foreground="White"
                   FontSize="28"
                   />
        <ListView ItemsSource="{Binding WindowHandlers}">
            <ListView.View>
                <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Employee Information">
                    <GridViewColumn DisplayMemberBinding="{Binding Path=handlerName}" Header="Name"/>
                </GridView>

            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
